import {Link} from "react-router-dom";

const Account = () => {
    return (
        <div>
            <h1>Account</h1>
            <Link to="/">Home</Link>
        </div>
    );
};

export default Account;